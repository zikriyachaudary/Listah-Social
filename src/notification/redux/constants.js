import { actionGenerator } from '../../util/reduxHelpers';

export const GET_NOTIFICATIONS = actionGenerator(
  'GET_NOTIFICATIONS',
);

export const DELETE_NOTIFICATION = actionGenerator(
  'DELETE_NOTIFICATION',
);
